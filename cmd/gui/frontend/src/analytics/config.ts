// Aptabase App Key (Aptabase dashboard → App Settings). The region is encoded in the prefix (A-EU / A-US), so no host
// override is needed for Aptabase Cloud.
export const aptabaseAppKey = 'A-EU-7340688638';
