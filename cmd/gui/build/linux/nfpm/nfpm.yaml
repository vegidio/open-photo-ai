# Feel free to remove those if you don't want/need to use them.
# Make sure to check the documentation at https://nfpm.goreleaser.com
#
# The lines below are called `modelines`. See `:help modeline`

name: "blah"
arch: ${GOARCH}
platform: "linux"
version: "0.1.0"
section: "default"
priority: "extra"
maintainer: ${GIT_COMMITTER_NAME} <${GIT_COMMITTER_EMAIL}>
description: "An open source photo AI editor"
vendor: "Ascentiun AB"
homepage: "https://wails.io"
license: "AGPL-3.0"
release: "1"

contents:
  - src: "./bin/blah"
    dst: "/usr/local/bin/blah"
  - src: "./build/appicon.png"
    dst: "/usr/share/icons/hicolor/128x128/apps/blah.png"
  - src: "./build/linux/blah.desktop"
    dst: "/usr/share/applications/blah.desktop"

# Default dependencies for Debian 13/Ubuntu 24.04+ with GTK4 + WebKitGTK 6.0
depends:
  - libgtk-4-1
  - libwebkitgtk-6.0-4

# Distribution-specific overrides for different package formats and WebKit versions
overrides:
  # RPM packages for Fedora/RHEL/AlmaLinux/Rocky Linux (GTK4 + WebKitGTK 6.0)
  rpm:
    depends:
      - gtk4
      - webkitgtk6.0

  # Arch Linux packages (GTK4 + WebKitGTK 6.0)
  archlinux:
    depends:
      - gtk4
      - webkitgtk-6.0

# scripts section to ensure desktop database is updated after install
scripts:
  postinstall: "./build/linux/nfpm/scripts/postinstall.sh"
  # You can also add preremove, postremove if needed
  # preremove: "./build/linux/nfpm/scripts/preremove.sh"
  # postremove: "./build/linux/nfpm/scripts/postremove.sh"

# replaces:
#   - foobar
# provides:
#   - bar
# depends:
#   - gtk3
#   - libwebkit2gtk
# recommends:
#   - whatever
# suggests:
#   - something-else
# conflicts:
#   - not-foo
#   - not-bar
# changelog: "changelog.yaml"
